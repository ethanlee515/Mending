# Scheme Utilities

Small building blocks needed to define cryptographic schemes and reductions live here.

Keep this directory narrow: if a helper becomes generally useful for MathComp, SSProve, or probability reasoning, move it to `LibExtras/` or `Probability/` instead.
